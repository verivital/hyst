system = in
initially = "x == -5 & y == 0"
output-variables = "x, y"
scenario = "supp"
directions = "uni32"

sampling-time = 0.1
time-horizon = 3.2
iter-max = 5
output-format = "GEN"

forbidden = "x >= 6.5 || x <= -10"
