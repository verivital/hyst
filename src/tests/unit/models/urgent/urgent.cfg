# Composability Tests - base component only
system = network
initially = "loc(system_1)==one & loc(listener_1)==first & x == 0 & y == 0"
sampling-time = 0.1
time-horizon = 25
output-variables = "x, y"

