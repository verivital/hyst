# Composability Tests - base component only
system = network
initially = "loc(system_1)==init & loc(listener_1)==init & x == 0 & y == 0"
sampling-time = 0.1
time-horizon = 25
output-variables = "x, y"

